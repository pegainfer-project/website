---
title: pegainfer
description: "Pure Rust + CUDA LLM inference engine — no PyTorch, OpenAI-compatible, serving models from Qwen3 to the trillion-parameter Kimi-K2."
# Starlight renders the homepage <title> as "pegainfer | pegainfer"
# (page title | site title). Override it with a descriptive one so the
# homepage also ranks for category terms, not just the brand name.
head:
  - tag: title
    content: "pegainfer — Pure Rust + CUDA LLM Inference Engine"
  - tag: meta
    attrs:
      property: og:title
      content: "pegainfer — Pure Rust + CUDA LLM Inference Engine"
---

A from-scratch LLM inference engine in pure Rust + CUDA. OpenAI-compatible. Serves Qwen3 to Kimi-K2.

## Highlights

- **No Python at runtime** — no PyTorch, no ONNX, no framework in the serving path.
- **Model lines from 4B dense to trillion-parameter MoE** — Qwen3 through Kimi-K2 and GLM-5.2, one engine each.
- **Multi-node expert parallelism and P/D disaggregation** for large MoE deployments.
- **OpenAI-compatible API** — point any SDK at `http://localhost:8000/v1`.

See the [getting started guide](/getting-started/) or the [GitHub repository](https://github.com/pegainfer-project/pegainfer).

## Quick start

```bash
git clone https://github.com/pegainfer-project/pegainfer
cd pegainfer
cargo run --release -- --model-path models/Qwen3-4B
```

Then send a request:

```bash
curl -s http://localhost:8000/v1/completions \
  -H "Content-Type: application/json" \
  -d '{"prompt": "The capital of France is", "max_tokens": 32}'
```

## Blog

- **[Speculative Decoding](/blog/speculative-decoding/)** (2026-07-17) — From output entropy and verification correctness to EAGLE draft models and dynamic verify length.
- **[See Qwen3 Decode as a CUDA Graph](/blog/cuda-graph-export/)** (2026-07-10) — Export the live decode graph as a detailed DOT and a folded high-resolution PNG.
- **[Co-locating Prefill and Decode on One GPU](/blog/green-ctx/)** (2026-06-20) — CUDA Green Contexts for stable decode latency without losing throughput.
- **[PegaInfer 0.1.0: Production-Grade Rust Inference](/blog/pegainfer-010/)** (2026-06-13) — Architecture, footprint, cold-start, and RTX 5090 serving benchmarks.

## Footprint

No PyTorch, no JIT, no runtime compilation. The whole stack — weight loading, paged KV cache, schedulers, kernels — is Rust + CUDA from scratch. Resident memory stays a fraction of vLLM's at the same load.

![Process RSS: pegainfer vs vLLM](/blog/pegainfer-010/rss.png)

*Process RSS · pegainfer vs vLLM · Qwen3-4B BF16*

## Startup

3s warm start vs vLLM's 32.7s — about 11×. No runtime compilation, no JIT warmup: once it's built, what ships is stable and predictable.

![Cold start: pegainfer vs vLLM](/blog/pegainfer-010/startup.png)

*Warm start · pegainfer 3.0s vs vLLM 32.7s*

## Throughput

Same vLLM bench-serve client, same Poisson arrivals, same seed on RTX 5090. At the saturation point pegainfer pushes 6% higher throughput than vLLM 0.22.1.

![Serving sweep vs vLLM on RTX 5090](/blog/pegainfer-010/qps.png)

*Serving sweep · RTX 5090 · vs vLLM 0.22.1*

## Decode

Decode is captured as a CUDA graph with pre-allocated buffers, keeping per-token overhead low and decode latency flat across context lengths.

![Warm prefix-cache-hit TTFT sweep](/blog/pegainfer-010/warm_ttft.png)

*Warm prefix-cache TTFT · by input length*

## Supported models

| Model | Architecture |
| --- | --- |
| [Qwen3-4B / 8B / 14B / 32B](/models/qwen3-4b/) | Full attention, tensor parallel |
| Qwen3.5-4B | Hybrid: 24 linear + 8 full attention layers |
| DeepSeek-V4 | MoE + compressor + indexer, 8-GPU |
| DeepSeek-V2-Lite | MoE + expert parallelism, 2-GPU |
| Kimi-K2 | MLA + MoE + Marlin INT4, 8-GPU expert parallelism |
